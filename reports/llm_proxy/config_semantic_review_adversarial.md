# LP Panel CP1 — Config Semantic Review (LP-B Adversarial)

> **Backfilled in Phase 10.** See note in `config_semantic_review_grandmaster.md`.
> **lp_b_simulated: TRUE** (HANDOVER §2.2 — generated by Claude rather than a different
> model family; a real-release run must re-execute in a fresh Gemini/GPT-class session.)

**Checkpoint:** CP1 (config_semantic_review)
**LP role:** LP-B Adversarial
**Inputs:** P18 grandmaster output + 8 config YAMLs + `config_schema_validation_report.md`.

---

## Attack axis 1 — Q-code coverage gaps

- Q15 has 4 sub-codes (A/B/C/D).  Could one of them be silently mis-mapped to an axis
  where it shouldn't trigger?  Inspection: each Q15 subcode maps to a specific A5/A8/A9
  axis state.  No cross-leakage.
- Q19 (immunogenicity) requires endpoint_data_type=IMMUNOGENICITY for activation.
  This is correctly gated by axis A0.  No false-positive route.

**Severity:** none.

## Attack axis 2 — Forced node enforcement at config layer

- The 7 forced nodes (N0, N1, N2, N3, N4, N5, N8) all carry `cost_if_excluded: INFINITY`.
  ILP cannot exclude them.
- However: N8 (AIC-policy completeness) is v5.1-NEW (PATCH-3).  Its `q_code_if_fail` is
  the placeholder `(Q-code per policy)`, not a concrete code.  This forced the build
  step (P102) to normalize to Q15A.  A regulator reading the config alone might be
  confused by the placeholder.

**Severity:** minor.  Recommend documenting the Q15A normalization in
`candidate_node_dictionary_with_costs.csv`'s comment or in a top-level note in
`quarantine_reason_codes.yaml`.  Not a blocker for v1.0.

## Attack axis 3 — Action function library completeness

- `action_function_library.yaml` lists 38 functions.  `repair_rule_dictionary.yaml`
  rules cover 26.  Are the remaining 12 truly all core/utility?  Yes — `parse_source`,
  `assign_evid`, `sort_records`, `export_nonmem_ready`, `flag_quarantine`,
  `flag_invalid`, `flag_unsupported`, `assign_cmt_single`, `assign_cmt_multi`,
  `remove_exact_duplicates`, `attach_covariate_baseline`, `attach_covariate_time_varying`.
  All present and non-controversial.

**Severity:** none.

## Attack axis 4 — F30 reservation

- F30 reserved but no config artifact says *why*.  A v1.1 contributor could re-use the
  number, breaking the CHECK-11 PATCH-C4 defense.

**Severity:** minor.  Recommend adding a single-line comment in
`family_assignment_rules.yaml` stating "F30 reserved; do not reuse — see PATCH-C4".

## Attack axis 5 — AIC template completeness

- `analysis_intent_contract_template.yaml` has 8 required v4.2-NEW fields.  Are any
  silently optional?  Inspection shows all are flagged `required: true` for the
  corresponding endpoint_data_type/modality.  No silent optionality.

**Severity:** none.

## Attack axis 6 — Axis state value sanity

- `axis_dictionary.yaml` enumerates allowed states per axis.  Cross-check with the
  scenario universe: every state used in `data/scenario_universe/scenario_universe_v1.0.csv`
  is declared in the dictionary.  Confirmed via the 22-validator at Phase 1.

**Severity:** none.

## Attack axis 7 — Schema drift between locked artifacts

- All locked YAMLs have SHA256 anchors (in CHANGELOG); none drifted since lock.

**Severity:** none.

---

## Summary

| axis | severity | description | recommended correction |
|---|---|---|---|
| 2 | minor | N8 Q-code placeholder unexplained at config layer | document Q15A normalization |
| 4 | minor | F30 reservation not explained in config | add comment in family_assignment_rules.yaml |

**No fatal issues**. Two minor documentation nits, both v1.1 housekeeping.
Recommendation: **APPROVE** at LP-C.

*— End of CP1 LP-B adversarial output (lp_b_simulated=TRUE) —*
